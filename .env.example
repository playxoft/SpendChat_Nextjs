# MoneyTracker environment variables.
# Secrets are managed by Doppler — this file documents what is required.
# For local-without-Doppler dev, copy to `.env.local` and fill in values.

# --- Neon Postgres ---------------------------------------------------------
# Pooled connection string from the Neon console (sslmode=require).
NEON_POSTGRES_DATABASE_URL="postgresql://user:password@ep-xxxx-pooler.region.aws.neon.tech/neondb?sslmode=require"

# --- Neon Auth (@neondatabase/auth) ----------------------------------------
# Auth URL from the Neon console: Project -> Auth -> Configuration.
NEON_AUTH_BASE_URL="https://ep-xxxx.neonauth.region.aws.neon.tech/neondb/auth"
# Secret for signing session cookies. Generate with: openssl rand -base64 32
NEON_AUTH_COOKIE_SECRET="replace-with-a-32+-character-random-secret"

# --- Site ------------------------------------------------------------------
# Canonical public URL, used for SEO/canonical/OpenGraph and sitemap.
NEXT_PUBLIC_SITE_URL="https://moneytracker.playxoft.com"
