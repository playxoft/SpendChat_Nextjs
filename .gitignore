# ---- dependencies ----
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# ---- next.js ----
/.next/
/out/
/build
next-env.d.ts

# ---- testing / coverage ----
/coverage

# ---- env & secrets (managed by Doppler — NEVER commit) ----
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
# Firebase service-account key (migration script only)
.firebase-service-account.json

# ---- cloudflare / opennext / wrangler ----
/.open-next/
/.wrangler/
wrangler.*.local
cloudflare-env.d.ts

# ---- typescript ----
*.tsbuildinfo

# ---- logs / debug ----
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ---- editors / IDE ----
.idea/
*.iml
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# ---- OS ----
.DS_Store
Thumbs.db
*.pem

# ---- misc ----
.vercel

# ---- local-only, never committed (review reports, scratch notes, etc.) ----
_developer/local-only/

# /design-sync (Claude Design system sync)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
.design-sync/ds-entry.ts
/dist
